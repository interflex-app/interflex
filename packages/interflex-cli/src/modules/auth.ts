import { type InterflexConfig } from "interflex-core";
import { warning } from "../cli";

export const login = async (config: InterflexConfig) => {
  warning("Not implemented yet.");
};

export const link = async (config: InterflexConfig) => {
  warning("Not implemented yet.");
};
